- hosts: localhost
  connection: local
  become: true
  vars_files:
    - vars/jenkins.yml
    - vars/site_specific.yml
  roles:
    - geerlingguy.jenkins
    - openstack.jenkins-job-builder
    - octarine.postgresql
    - digi2al.jenkins
    - digi2al.python
    - digi2al.phantomjs
    - digi2al.dependencies
