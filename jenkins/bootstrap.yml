- hosts: jenkins
  sudo: true
  vars_files:
    - vars/jenkins.yml
    - "{{ secrets_vars }}"
  roles:
    - geerlingguy.jenkins
    - openstack.jenkins-job-builder
    - octarine.postgresql
    - digi2al.jenkins
    - digi2al.python
    - digi2al.phantomjs
    - digi2al.dependencies
